# v21.10 Draft Confirmation Hotfix

Исправлено:
- автопилот теперь отправляет inline-кнопки к созданному черновику;
- /queue @channel показывает не только scheduled posts, но и активные draft-черновики;
- добавлены команды /draft <id>, /publish <id>, /approve <id>;
- публикация через команду проходит те же проверки прав и ALLOWED_CHANNELS.
